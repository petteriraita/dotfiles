[moonshine]
language = "en"
model_arch = "medium-streaming"
# Moonshine revises the active line at this interval while speech arrives.
update_interval_seconds = 0.5
sample_rate = 16000
channels = 1
block_size = 1024
startup_timeout_seconds = 30.0
request_timeout_seconds = 60.0

