[Unit]
Description=Kanata keyboard remapper
After=systemd-udev-settle.service

[Service]
ExecStartPre=/sbin/modprobe uinput
ExecStart=/usr/bin/kanata --cfg /home/pt/.config/kanata/config.kbd
Restart=on-failure
RestartSec=1

[Install]
WantedBy=multi-user.target
